{
    "name": "19_web_product_pricing",
    "version": "1.0.0",
    "description": "Product Pricing Web Application",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
        "start": "npm start --prefix backend"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/kenethcarajay2-create/product_pricing.git"
    },
    "license": "ISC",
    "devDependencies": {
        "concurrently": "^9.0.0"
    }
}